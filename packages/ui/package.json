{
  "name": "@szl/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared UI (NavBar, tool link constants) reused across the Website, Analyzer, Match Builder, and Calculator apps. Consumed via Vite resolve.alias pointing at src/ (no build step).",
  "main": "src/index.js"
}
