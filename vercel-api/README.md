# SparkingZero API Proxy

Serverless functions for GitHub Device Flow OAuth proxy.

## Deploy to Vercel

1. Create a new Vercel project
2. Import this `vercel-api/` directory as the root
3. Deploy

Functions will be available at:
- `/api/github-device-start`
- `/api/github-device-token`
